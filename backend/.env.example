# Copy to .env and set your real values. Do not commit .env.
# Database (PostgreSQL)
DB_NAME=dmis
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# Optional: use SQLite for local dev (no PostgreSQL required)
# DJANGO_USE_SQLITE=1

# Optional: Django
# DJANGO_SECRET_KEY=your-secret-key
# DJANGO_DEBUG=1
# DEV_AUTH_ENABLED=1
